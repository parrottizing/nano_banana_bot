# Project Workflow Rules

1. Current active workflow is TypeScript + Cloudflare Workers + D1.
   Python workflow is legacy and should not be used unless explicitly requested.

2. For requested code changes:
   Run the relevant tests, and if tests pass, deploy the change automatically so it can be tested immediately.
